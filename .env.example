# PostgreSQL connection string (required).
# Get one free at https://neon.tech — see README for full instructions.
DATABASE_URL=""

# Anthropic API key for Claude (required).
# Sign up at https://console.anthropic.com — $5 free credit on new accounts.
ANTHROPIC_API_KEY=""

# Google Places API key (optional, enables RAG mode for venue grounding).
# Without this, the AI guide falls back to parametric LLM knowledge with
# anti-hallucination prompts. See README for setup instructions.
GOOGLE_PLACES_API_KEY=""
