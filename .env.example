# Database (Neon Postgres) — get from neon.tech
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Anthropic Claude — get from console.anthropic.com
ANTHROPIC_API_KEY="sk-ant-..."

# Google Places API (New) — enable "Places API (New)" + billing in Google Cloud.
# Optional: when unset, guide generation falls back to ungrounded mode.
GOOGLE_PLACES_API_KEY=""
